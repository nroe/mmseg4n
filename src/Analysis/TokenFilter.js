/**
 * 词
 * 
 * @author Nroe
 * @package MMSeg4N.Analysis
 */
Analysis_TokenFilter = new JS.Class({
    /**
     * 过滤或删除 TOKEN
     * 
     * @param Analysis_Token token
     * @return token|null
     */
    normalize: function(srcToken)
    {
    },
});